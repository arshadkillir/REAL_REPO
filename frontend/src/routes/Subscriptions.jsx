import React, { useState } from 'react'; import axios from 'axios'; export default function Subscriptions(){ const [tenantId,setTenantId]=useState('1'); const [plan,setPlan]=useState('pro'); async function start(){ const res = await axios.post((import.meta.env.VITE_API_URL||'http://localhost:4000') + '/api/billing/create-checkout-session', { tenantId, plan }); if(res.data.url) window.location = res.data.url; } return (<div style={{padding:20}}><h2>Subscriptions</h2><div>Tenant ID: <input value={tenantId} onChange={e=>setTenantId(e.target.value)} /></div><div>Plan: <select value={plan} onChange={e=>setPlan(e.target.value)}><option value='basic'>Basic</option><option value='pro'>Pro</option></select></div><button onClick={start}>Checkout</button></div>); }