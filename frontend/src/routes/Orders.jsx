import React, { useEffect, useState } from 'react'; import api from '../utils/api'; export default function Orders(){ const [menu,setMenu]=useState([]); useEffect(()=>{ api.get('/menu').then(r=>setMenu(r.data||[])); },[]); return (<div style={{padding:20}}><h2>Orders</h2><div style={{display:'flex',flexWrap:'wrap'}}>{menu.map(m=>(<div key={m.id} style={{background:'#fff',padding:12,margin:8,borderRadius:8,width:220}}>{m.name}<div>${m.price}</div></div>))}</div></div>); }