import React, { useEffect, useState } from 'react'; import api from '../utils/api'; export default function Inventory(){ const [ings,setIngs]=useState([]); useEffect(()=>{ api.get('/inventory/ingredients').then(r=>setIngs(r.data||[])); },[]); return (<div style={{padding:20}}><h2>Inventory</h2><div>{ings.map(i=>(<div key={i.id} style={{background:'#fff',padding:8,margin:8,borderRadius:6}}>{i.name} - {i.stock} {i.unit}</div>))}</div></div>); }