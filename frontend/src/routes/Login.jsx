import React, { useState } from 'react'; import axios from 'axios'; export default function Login(){ const [email,setEmail]=useState('admin@demo.example'); const [password,setPassword]=useState('admin123'); async function doLogin(){ const res = await axios.post((import.meta.env.VITE_API_URL||'http://localhost:4000') + '/api/auth/login', { email, password }); localStorage.setItem('token', res.data.token); alert('Logged in'); } return (<div style={{padding:20}}><h2>Login</h2><div>Email: <input value={email} onChange={e=>setEmail(e.target.value)} /></div><div>Password: <input value={password} onChange={e=>setPassword(e.target.value)} /></div><button onClick={doLogin}>Login</button></div>); }