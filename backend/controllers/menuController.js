const { MenuItem } = require('../models'); async function listMenu(req,res){ const tenantId = req.tenantId || req.query.tenantId || null; const where = {}; if(tenantId) where.tenantId = tenantId; res.json(await MenuItem.findAll({ where })); } async function createMenu(req,res){ const tenantId = req.tenantId || req.body.tenantId || null; const m = await MenuItem.create({ ...req.body, tenantId }); res.json(m); } module.exports = { listMenu, createMenu };