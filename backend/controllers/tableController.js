const { Table } = require('../models'); async function listTables(req,res){ const tenantId = req.tenantId || req.query.tenantId || null; const where = {}; if(tenantId) where.tenantId = tenantId; res.json(await Table.findAll({ where })); } async function createTable(req,res){ const tenantId = req.tenantId || req.body.tenantId || null; const t = await Table.create({ ...req.body, tenantId }); res.json(t); } module.exports = { listTables, createTable };