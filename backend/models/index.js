const { DataTypes } = require('sequelize'); const { sequelize } = require('../config/database'); const Tenant = require('./tenant')(sequelize, DataTypes); const User = require('./user')(sequelize, DataTypes); const MenuItem = require('./menuItem')(sequelize, DataTypes); const Table = require('./table')(sequelize, DataTypes); const Order = require('./order')(sequelize, DataTypes); const OrderItem = require('./orderItem')(sequelize, DataTypes); Order.hasMany(OrderItem, { foreignKey: 'orderId' }); module.exports = { sequelize, Tenant, User, MenuItem, Table, Order, OrderItem };